# Session 3: Exercise

## Question

Create a systemd service that schedules a task, the task does the following:

1. Checks for all *.pdf files within the `~/Downloads` directory
2. Moves any files detected under the `$HOME/archive` directory
3. Runs each 2 minutes

**Note:** Do not use cronjobs, search for alternative way.

Submit a PDF report outlining the steps you followed.